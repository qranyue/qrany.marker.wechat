<!--index.wxml-->
<map id="map" subkey="{{ MAP_KEY }}" latitude="{{ latitude }}" longitude="{{ longitude }}" markers="{{ markers }}" bindmarkertap="onMarkerTap"></map>

<view id="center"></view>

<view id="footer">
  <lib-push loading="{{ add_loading }}" bind:tap="onTap"></lib-push>
</view>
