/* lib/back.wxss */

:host {
  display: block;
  position: fixed;
  left: 20rpx;
  top: calc(20rpx + constant(safe-area-inset-bottom));
  top: calc(20rpx + env(safe-area-inset-bottom));
}

.back {
  padding: 24rpx;
}

.icon-left {
  line-height: 1;
}

.back:not([size="mini"]) {
  width: auto;
}
