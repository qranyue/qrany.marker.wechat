/* lib/back.wxss */

:host {
  display: block;
  position: fixed;
  left: 20rpx;
  top: var(60rpx + var(--status-bar-height, 100rpx));
}

.back {
  padding: 24rpx;
}

.icon-left {
  line-height: 1;
}

.back:not([size="mini"]) {
  width: auto;
}
