<!--lib/button.wxml-->
<button class="button" type="{{ type }}" form-type="{{ form }}" disabled="{{ disabled || loading }}" bindtap="{{ onClick }}">
  <lib-spin wx:if="{{ loading }}" size="{{ 12 }}"></lib-spin>
  <text><slot></slot></text>
</button>
